{% extends 'base.html.twig' %}

{% block title %}{{ 'header.user.change_password' | trans }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ 'header.user.change_password' | trans }}</h1>

        {{ form_start(form) }}
            {{ form_rest(form) }}
            <button type="submit" class="btn btn-primary">{{ 'link.save' | trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
