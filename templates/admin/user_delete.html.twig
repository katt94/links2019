{% extends 'base.html.twig' %}

{% block title %}{{ 'header.user.delete' | trans }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ 'header.user.delete' | trans }}</h1>

        {{ form_start(form, { method: 'DELETE', action: url('admin_user_delete', { id: user.id }) }) }}
            {{ form_widget(form) }}
            <div class="form-group float-right">
                <button type="submit" class="btn btn-danger">{{ 'link.remove_user' | trans }}</button>
            </div>
            <div class="form-group">
                <a href="{{ url('admin_user_list') }}" class="btn btn-link">
                    {{ 'link.back_to_list' | trans }}
                </a>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
