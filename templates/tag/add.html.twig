{% extends 'base.html.twig' %}

{% block title %}{{ 'header.tag.add' | trans }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ 'header.tag.add' | trans }}</h1>

        {{ form_start(form) }}
            {{ form_rest(form) }}
            <button type="submit" class="btn btn-primary">{{ 'link.add' | trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
